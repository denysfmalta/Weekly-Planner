export type inputProps = {
  labelTitle: string;
  placeholder: string;
  type: string;
  value: string;
  setValue: any;
  error: boolean;
};

export type buttonProps = {
  buttonName: string
};

export type errorProps = {
  fieldname: string
}

export type homeProps = {
  subtitle: string
}