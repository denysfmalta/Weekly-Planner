export type createData = {
  firstName: string;
  lastName: string;
  birthDate: string;
  city: string;
  country: string;
  email: string;
  password: string;
  confirmPassword: string;
};


export type userLog = {
  email: string
  password: string
}

export type eventData = {
  description: string
  dayOfWeek: string
}